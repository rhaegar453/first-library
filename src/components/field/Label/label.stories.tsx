import React from "react";
import Label from ".";

export default {
  title: "Atoms/Label",
  component: Label,
};

export const withDefault = (args: any) => (
  <Label>Hello World this is a Label</Label>
);
